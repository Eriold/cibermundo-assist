{
  "name": "interrpa",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.3",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "env:init": "pnpm -r env:init",
    "doc": "pnpm -C app/backend doc"
  }
}
